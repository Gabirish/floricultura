@model FloriculturaBeta.Models.Venda

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Venda</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Cliente.ClienteNome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Cliente.ClienteNome)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Funcionario.FuncionarioNome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Funcionario.FuncionarioNome)
        </dd>
    </dl>

        <table class="table">
            <tr>
                <th>
                   Produto
                </th>
                <th>
                   Quantidade
                </th>
            </tr>

            @foreach (var item in Model.ItensVenda)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Produto.ProdutoNome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ItemVendaQuantidade)
                    </td>
                  
                  
                </tr>
            }

        </table>


</div>
<p>

    @Html.ActionLink("Back to List", "Index")
</p>
